# Grafana provisioning — cargar dashboards desde ficheros
apiVersion: 1

providers:
  - name: 'ysh'
    orgId: 1
    folder: 'YSH'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
